# Comparison

## H0003

The Controller entrypoint was visible, but Decision evidence references still
looked like unconstrained strings.

## H0004

The Tool schema matches the runtime routing whitelist.

In the paired run, format rejection fell from 2/6 Decision attempts to 0/4,
while both conditions used one physical trial. This isolates protocol
conversion rather than task outcome.
