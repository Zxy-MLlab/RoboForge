# Change

Not implemented. Planned minimal surface:

- explicit optional `physical_verification` in AgentEvidence;
- set only for committed physical trials after receipt validation;
- preserve it in bounded evidence summaries;
- no Adapter, Prompt, Decision, reset, Controller, or capability-policy change.
